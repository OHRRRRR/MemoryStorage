/**
 * @format
 */

import {AppRegistry} from 'react-native';
import App from './App';
import {name as appName} from './app.json';
import SaveMemoryAge from './pages/SaveMemoryAge';

AppRegistry.registerComponent(appName, () => SaveMemoryAge);
